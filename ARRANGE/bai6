#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a[10],n,x,k=0;
   scanf("%d",&n);
   for(int i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
       if(a[i]<0)
        a[i]=0;
   }
   for(int i=0;i<n;i++)
   {
       printf("%d ",a[i]);
   }

    return 0;
}
