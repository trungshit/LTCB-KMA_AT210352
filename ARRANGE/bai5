#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a[10],n,x,k=0;
   scanf("%d",&n);
   scanf("%d",&x);
   for(int i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
       if(a[i]==x)
        k++;
   }
    printf("%d xuat hien %d lan",x,k);

    return 0;
}
