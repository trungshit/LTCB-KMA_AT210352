#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,b;
    int a[10],n,x,k;
    scanf("%d",&n);
    for( i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
    for( i=0; i<n-1; i++)
    {
        x=a[i];
        for( j=i+1; j<n; j++)
        {
            if(a[j]<x)
            {
                b=j;
            }
        }
        if(a[i!=x])
        {
            k=a[i];
            a[i]=a[b];
            a[b]=k;
        }
    }
for(i=0;i<n;i++)
    printf("%d ",a[i]);
    return 0;
}
