
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,k;
    int a[10],b[10],c[10],n,m;
    scanf("%d %d",&n,&m);
    for( i=0; i<n*m; i++)
    {
        scanf("%d",&a[i]);
    }
for(i=0;i<m*n;i++)
{
 printf("%d ",a[i]);
 if((i+1)%m==0)
    printf("\n");
}
    return 0;
}
