#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,k=0;
    int a[10],b[10],c[10],n,m;
    scanf("%d %d",&n,&m);
    for( i=0; i<n*m; i++)
    {
        scanf("%d",&a[i]);
        k+=a[i]
    }

 printf("%d ",k);
 
    return 0;
}
