#include <stdio.h>
#include <stdlib.h>
void swap(int *a,int *b)
{
    int r=*a;
    *a=*b;
    *b=r;
}
int main()
{
    int a[10],b;
    scanf("%d",&b);
    for(int i=0;i<b;i++)
        scanf("%d",&a[i]);
     for(int i=0;i<b;i++)
        printf("%d ",a[i]);


    return 0;
}
